import { categoryColors, colors, gradients, statusColors } from './colors';
import { duration, easing, pressScale, springs, stagger } from './motion';
import { radii } from './radii';
import { shadows } from './shadows';
import { layout, spacing } from './spacing';
import { fontFamily, typography } from './typography';

export const theme = {
  colors,
  categoryColors,
  statusColors,
  gradients,
  typography,
  fontFamily,
  spacing,
  layout,
  radii,
  shadows,
  motion: { duration, easing, springs, stagger, pressScale },
};

export {
  categoryColors,
  colors,
  duration,
  easing,
  fontFamily,
  gradients,
  layout,
  pressScale,
  radii,
  shadows,
  spacing,
  springs,
  stagger,
  statusColors,
  typography,
};
